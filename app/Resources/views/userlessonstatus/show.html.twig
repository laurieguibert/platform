{% extends 'base.html.twig' %}

{% block body %}
    <h1>Userlessonstatus</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ userLessonStatus.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ userLessonStatus.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('userlessonstatus_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('userlessonstatus_edit', { 'id': userLessonStatus.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
